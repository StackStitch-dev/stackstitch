---
phase: 01-core-domain-ports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/pyproject.toml
  - core/src/core/__init__.py
  - core/src/core/domain/__init__.py
  - core/src/core/domain/enums.py
  - core/src/core/domain/exceptions.py
  - core/src/core/domain/events/__init__.py
  - core/src/core/domain/events/domain_events.py
  - core/src/core/domain/entities/__init__.py
  - core/src/core/domain/entities/stream.py
  - core/src/core/domain/entities/metric.py
  - core/src/core/domain/entities/insight.py
  - core/src/core/domain/entities/investigation.py
  - core/src/core/domain/entities/thread.py
  - core/src/core/domain/entities/invocation.py
  - core/src/core/application/__init__.py
  - core/src/core/application/ports/__init__.py
  - core/src/core/application/use_cases/__init__.py
  - core/tests/__init__.py
  - core/tests/conftest.py
  - core/tests/fakes/__init__.py
  - core/tests/domain/__init__.py
  - core/tests/use_cases/__init__.py
autonomous: true
requirements:
  - INFR-05

must_haves:
  truths:
    - "All domain entities can be instantiated with valid data and reject invalid data via Pydantic validation"
    - "Domain events are immutable frozen Pydantic models with event_id and occurred_at"
    - "Entity base class supports collect_event/flush_events pattern via PrivateAttr"
    - "Composite-key entities (Stream, Metric) use __eq__ and __hash__ based on their composite key"
    - "All enums are string enums that serialize naturally to JSON"
    - "Exception hierarchy carries typed context fields"
  artifacts:
    - path: "core/pyproject.toml"
      provides: "Project configuration with dependencies, pytest, ruff, mypy settings"
      contains: "asyncio_mode"
    - path: "core/src/core/domain/enums.py"
      provides: "All domain string enums"
      contains: "class InvestigationStatus"
    - path: "core/src/core/domain/exceptions.py"
      provides: "DomainError hierarchy with typed context"
      contains: "class DomainError"
    - path: "core/src/core/domain/events/domain_events.py"
      provides: "All 6 domain events as frozen Pydantic models"
      contains: "class StreamDataPointCreated"
    - path: "core/src/core/domain/entities/stream.py"
      provides: "Stream and StreamDataPoint"
      contains: "class Stream"
    - path: "core/src/core/domain/entities/investigation.py"
      provides: "Investigation, InvestigationStep, InvestigatorResult"
      contains: "class Investigation"
  key_links:
    - from: "core/src/core/domain/entities/*.py"
      to: "core/src/core/domain/events/domain_events.py"
      via: "Entity.collect_event imports DomainEvent"
      pattern: "from core\\.domain\\.events"
    - from: "core/src/core/domain/entities/*.py"
      to: "core/src/core/domain/enums.py"
      via: "entities use string enums for status/type fields"
      pattern: "from core\\.domain\\.enums import"
---

<objective>
Create the core/ project scaffold with all domain entities, value objects, enums, domain events, and exceptions -- the complete domain layer of the hexagonal architecture.

Purpose: Establish the stable domain contracts that all downstream phases (ports, use cases, adapters) build against. This is the foundation of the entire system.
Output: A working Python package with all domain types that pass instantiation tests and type checking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-domain-ports/01-CONTEXT.md
@.planning/phases/01-core-domain-ports/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffold, base types, enums, exceptions, and domain events</name>
  <files>
    core/pyproject.toml,
    core/src/core/__init__.py,
    core/src/core/domain/__init__.py,
    core/src/core/domain/enums.py,
    core/src/core/domain/exceptions.py,
    core/src/core/domain/events/__init__.py,
    core/src/core/domain/events/domain_events.py,
    core/src/core/application/__init__.py,
    core/src/core/application/ports/__init__.py,
    core/src/core/application/use_cases/__init__.py,
    core/tests/__init__.py,
    core/tests/conftest.py,
    core/tests/fakes/__init__.py,
    core/tests/domain/__init__.py,
    core/tests/use_cases/__init__.py
  </files>
  <read_first>
    .planning/phases/01-core-domain-ports/01-CONTEXT.md,
    .planning/phases/01-core-domain-ports/01-RESEARCH.md
  </read_first>
  <action>
    1. Initialize the core/ project using `uv init core/ --lib` then customize pyproject.toml:
       - name = "stackstitch-core"
       - requires-python = ">=3.12"
       - dependencies: ["pydantic>=2.12,<3"]
       - dev dependencies: ["pytest>=9.0,<10", "pytest-asyncio>=0.24,<2", "pytest-cov>=6.0,<7", "ruff>=0.15,<1", "mypy>=1.20,<2"]
       - [tool.pytest.ini_options]: asyncio_mode = "auto", testpaths = ["tests"]
       - [tool.ruff]: target-version = "py312", line-length = 100
       - [tool.ruff.lint]: select = ["E", "F", "I", "N", "UP", "B", "SIM", "TCH"]
       - [tool.mypy]: python_version = "3.12", strict = true, plugins = ["pydantic.mypy"]
       - src layout: packages = [{include = "core", from = "src"}]
       - Run `cd core && uv sync` to install all dependencies

    2. Create all __init__.py files for the directory structure:
       - core/src/core/__init__.py (empty)
       - core/src/core/domain/__init__.py (empty)
       - core/src/core/domain/entities/__init__.py (re-exports all entity classes)
       - core/src/core/domain/events/__init__.py (re-exports all events)
       - core/src/core/application/__init__.py (empty)
       - core/src/core/application/ports/__init__.py (empty)
       - core/src/core/application/use_cases/__init__.py (empty)
       - core/tests/__init__.py, core/tests/fakes/__init__.py, core/tests/domain/__init__.py, core/tests/use_cases/__init__.py (all empty)

    3. Create core/src/core/domain/enums.py with ALL domain enums per D-43 (str, Enum):
       - StreamType: PULL_REQUEST = "pull_request", COMMIT = "commit", REVIEW = "review"
       - MetricType: PR_CYCLE_TIME = "pr_cycle_time", PR_THROUGHPUT = "pr_throughput", REVIEW_TURNAROUND_TIME = "review_turnaround_time"
       - InvestigationStatus: PENDING = "pending", RUNNING = "running", COMPLETED = "completed", FAILED = "failed"
       - InvestigationTrigger: ANOMALY = "anomaly", ADHOC = "adhoc"
       - InsightType: ANOMALY_EXPLANATION = "anomaly_explanation", PATTERN_DETECTION = "pattern_detection", AD_HOC_RESPONSE = "ad_hoc_response"
       - InvocationSource: USER_MESSAGE = "user_message", INSIGHT = "insight"
       - InvocationStatus: PENDING = "pending", PROCESSING = "processing", DONE = "done"
       - InvestigationStepType: TOOL_CALL = "tool_call", REASONING = "reasoning", OBSERVATION = "observation"
       - MessageRole: USER = "user", ASSISTANT = "assistant", SYSTEM = "system"
       - AnomalySeverity: LOW = "low", MEDIUM = "medium", HIGH = "high", CRITICAL = "critical"

    4. Create core/src/core/domain/exceptions.py per D-45, D-46, D-47:
       - DomainError(Exception): __init__(self, message: str, **context: Any), stores message and context dict
       - EntityNotFoundError(DomainError): __init__(self, entity_type: str, entity_id: UUID)
       - InvalidEntityStateError(DomainError): __init__(self, entity_type: str, entity_id: UUID, reason: str)
       - DuplicateEntityError(DomainError): __init__(self, entity_type: str, identifier: str)
       - ValidationError(DomainError): __init__(self, entity_type: str, field: str, reason: str)

    5. Create core/src/core/domain/events/domain_events.py with all 6 events per D-54 through D-59:
       - Use `from __future__ import annotations` for forward reference safety
       - DomainEvent(BaseModel) base: model_config = ConfigDict(frozen=True), event_id: UUID = Field(default_factory=uuid4), occurred_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))
       - StreamDataPointCreated(DomainEvent): source: str, stream_type: str, project_id: UUID, timestamp: datetime, data: dict[str, Any]
         NOTE: Carry the raw fields (not the StreamDataPoint object) to avoid circular imports -- the data point is reconstructed by the consumer
       - StreamUpdated(DomainEvent): source: str, stream_type: str, project_id: UUID
       - MetricUpdated(DomainEvent): metric_type: str, project_id: UUID
       - AnomalyDetected(DomainEvent): metric_type: str, project_id: UUID, severity: str, description: str, metric_value: float, threshold: float
       - InsightCreated(DomainEvent): insight_id: UUID, investigation_id: UUID, project_id: UUID
       - MessageCreated(DomainEvent): thread_id: UUID, message_content: str

    6. Create core/tests/conftest.py with a basic fixture placeholder (will be populated with fakes in Plan 02).
  </action>
  <verify>
    <automated>cd /Users/jorgeandresdiaz/Documents/development/stackstitch/core && uv run python -c "from core.domain.enums import InvestigationStatus, StreamType, MetricType; from core.domain.exceptions import DomainError, EntityNotFoundError; from core.domain.events.domain_events import DomainEvent, StreamDataPointCreated, AnomalyDetected; print('All imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - core/pyproject.toml contains `asyncio_mode = "auto"`
    - core/pyproject.toml contains `pydantic` in dependencies
    - core/pyproject.toml contains `pytest` in dev-dependencies
    - core/pyproject.toml contains `strict = true` under [tool.mypy]
    - core/src/core/domain/enums.py contains `class InvestigationStatus(str, Enum):`
    - core/src/core/domain/enums.py contains `class StreamType(str, Enum):`
    - core/src/core/domain/enums.py contains `class AnomalySeverity(str, Enum):`
    - core/src/core/domain/exceptions.py contains `class DomainError(Exception):`
    - core/src/core/domain/exceptions.py contains `self.context = context`
    - core/src/core/domain/events/domain_events.py contains `class DomainEvent(BaseModel):`
    - core/src/core/domain/events/domain_events.py contains `class StreamDataPointCreated(DomainEvent):`
    - core/src/core/domain/events/domain_events.py contains `class AnomalyDetected(DomainEvent):`
    - core/src/core/domain/events/domain_events.py contains `class InsightCreated(DomainEvent):`
    - core/src/core/domain/events/domain_events.py contains `class MessageCreated(DomainEvent):`
    - core/src/core/domain/events/domain_events.py contains `frozen=True`
    - All __init__.py files exist for: core/src/core/, domain/, entities/, events/, application/, ports/, use_cases/, tests/, fakes/, domain/ (test), use_cases/ (test)
    - `cd core && uv run python -c "from core.domain.enums import InvestigationStatus"` exits 0
  </acceptance_criteria>
  <done>All domain foundation types (enums, exceptions, events) importable. Project builds and resolves dependencies. Directory structure matches hexagonal layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Domain entities and value objects</name>
  <files>
    core/src/core/domain/entities/stream.py,
    core/src/core/domain/entities/metric.py,
    core/src/core/domain/entities/insight.py,
    core/src/core/domain/entities/investigation.py,
    core/src/core/domain/entities/thread.py,
    core/src/core/domain/entities/invocation.py,
    core/src/core/domain/entities/__init__.py,
    core/tests/domain/test_entities.py,
    core/tests/domain/test_events.py,
    core/tests/domain/test_exceptions.py
  </files>
  <read_first>
    .planning/phases/01-core-domain-ports/01-CONTEXT.md,
    .planning/phases/01-core-domain-ports/01-RESEARCH.md,
    core/src/core/domain/enums.py,
    core/src/core/domain/events/domain_events.py,
    core/src/core/domain/exceptions.py
  </read_first>
  <behavior>
    Entity tests (test_entities.py):
    - Stream instantiates with source="github", stream_type=StreamType.PULL_REQUEST, project_id=UUID, data_points=[]
    - Stream.add_data_point appends a StreamDataPoint to data_points list
    - Stream equality: two Streams with same (source, stream_type, project_id) are equal regardless of data_points
    - Stream with same composite key hashes to same value
    - StreamDataPoint is frozen (raises on attribute assignment)
    - Metric instantiates with metric_type=MetricType.PR_CYCLE_TIME, project_id=UUID, data_points=[]
    - Metric equality based on composite key (metric_type, project_id)
    - MetricDataPoint is frozen with value: float and timestamp: datetime
    - Investigation instantiates with all required fields, status defaults to PENDING
    - Investigation.start() transitions status PENDING -> RUNNING, raises InvalidEntityStateError if not PENDING
    - Investigation.complete(findings, tokens_used) transitions RUNNING -> COMPLETED, sets findings/tokens
    - Investigation.fail(reason) transitions RUNNING -> FAILED
    - Investigation collects and flushes domain events
    - InvestigationStep is frozen value object
    - InvestigatorResult is frozen value object with steps, findings, tokens_used
    - Insight instantiates with required fields, has metadata dict
    - Thread instantiates with id, project_id, empty messages list
    - Thread.add_message appends a Message
    - Message is frozen value object with role, content, timestamp
    - Invocation instantiates with source, role, message, status defaults to PENDING
    - Invocation.mark_processing() transitions PENDING -> PROCESSING
    - Invocation.mark_done() transitions PROCESSING -> DONE

    Event tests (test_events.py):
    - Each of 6 domain events can be instantiated
    - All events are frozen (assignment raises)
    - Events have auto-generated event_id and occurred_at

    Exception tests (test_exceptions.py):
    - DomainError stores message and context kwargs
    - EntityNotFoundError stores entity_type and entity_id in context
    - DomainError subclasses are catchable as DomainError
  </behavior>
  <action>
    Write tests first (RED), then implement entities (GREEN).

    1. Create core/tests/domain/test_entities.py with all entity behavior tests listed above.

    2. Create core/tests/domain/test_events.py with event instantiation and immutability tests.

    3. Create core/tests/domain/test_exceptions.py with exception hierarchy tests.

    4. Implement entity files:

    core/src/core/domain/entities/stream.py:
    - Use `from __future__ import annotations`
    - StreamDataPoint(BaseModel): model_config = ConfigDict(frozen=True), timestamp: datetime, data: dict[str, Any]
    - Stream(BaseModel): model_config = ConfigDict(validate_assignment=True), source: str, stream_type: StreamType, project_id: UUID, data_points: list[StreamDataPoint] = Field(default_factory=list)
    - Stream.add_data_point(self, data_point: StreamDataPoint) -> None
    - Stream.__eq__ based on (source, stream_type, project_id)
    - Stream.__hash__ based on (source, stream_type, project_id)

    core/src/core/domain/entities/metric.py:
    - MetricDataPoint(BaseModel): model_config = ConfigDict(frozen=True), value: float, timestamp: datetime
    - Metric(BaseModel): model_config = ConfigDict(validate_assignment=True), metric_type: MetricType, project_id: UUID, data_points: list[MetricDataPoint] = Field(default_factory=list)
    - Metric.add_data_point(self, data_point: MetricDataPoint) -> None
    - Metric.__eq__ and __hash__ based on (metric_type, project_id)

    core/src/core/domain/entities/investigation.py:
    - InvestigationStep(BaseModel): model_config = ConfigDict(frozen=True), step_type: InvestigationStepType, tool_name: str | None = None, input_data: dict[str, Any] = Field(default_factory=dict), output_data: dict[str, Any] = Field(default_factory=dict), reasoning: str = "", tokens_used: int = 0
    - InvestigatorResult(BaseModel): model_config = ConfigDict(frozen=True), steps: list[InvestigationStep], findings: str, tokens_used: int
    - Investigation(BaseModel): model_config = ConfigDict(validate_assignment=True), id: UUID = Field(default_factory=uuid4), project_id: UUID, trigger: InvestigationTrigger, trigger_ref: UUID, query: str | None = None, status: InvestigationStatus = InvestigationStatus.PENDING, steps: list[InvestigationStep] = Field(default_factory=list), findings: str | None = None, tokens_used: int = 0, created_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))
    - _events: list[DomainEvent] = PrivateAttr(default_factory=list) with collect_event/flush_events
    - start() -> None: validates status is PENDING, sets to RUNNING, raises InvalidEntityStateError otherwise
    - complete(result: InvestigatorResult) -> None: validates status is RUNNING, sets COMPLETED, copies steps/findings/tokens_used from result
    - fail(reason: str) -> None: validates status is RUNNING, sets FAILED, stores reason in findings

    core/src/core/domain/entities/insight.py:
    - Insight(BaseModel): model_config = ConfigDict(validate_assignment=True), id: UUID = Field(default_factory=uuid4), project_id: UUID, investigation_id: UUID, title: str, narrative: str, insight_type: InsightType, metadata: dict[str, Any] = Field(default_factory=dict), created_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))
    - _events: list[DomainEvent] = PrivateAttr(default_factory=list) with collect_event/flush_events

    core/src/core/domain/entities/thread.py:
    - Message(BaseModel): model_config = ConfigDict(frozen=True), role: MessageRole, content: str, timestamp: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))
    - Thread(BaseModel): model_config = ConfigDict(validate_assignment=True), id: UUID = Field(default_factory=uuid4), project_id: UUID, messages: list[Message] = Field(default_factory=list), created_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))
    - Thread.add_message(self, message: Message) -> None
    - _events with collect_event/flush_events

    core/src/core/domain/entities/invocation.py:
    - Invocation(BaseModel): model_config = ConfigDict(validate_assignment=True), id: UUID = Field(default_factory=uuid4), thread_id: UUID, project_id: UUID, source: InvocationSource, role: str, message: str, status: InvocationStatus = InvocationStatus.PENDING, created_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))
    - _events with collect_event/flush_events
    - mark_processing() -> None: validates PENDING, sets PROCESSING
    - mark_done() -> None: validates PROCESSING, sets DONE

    5. Update core/src/core/domain/entities/__init__.py to re-export all entity classes.

    6. Run tests -- all must pass.
  </action>
  <verify>
    <automated>cd /Users/jorgeandresdiaz/Documents/development/stackstitch/core && uv run pytest tests/domain/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - core/src/core/domain/entities/stream.py contains `class Stream(BaseModel):`
    - core/src/core/domain/entities/stream.py contains `class StreamDataPoint(BaseModel):`
    - core/src/core/domain/entities/stream.py contains `def add_data_point(`
    - core/src/core/domain/entities/stream.py contains `def __eq__(`
    - core/src/core/domain/entities/stream.py contains `def __hash__(`
    - core/src/core/domain/entities/metric.py contains `class Metric(BaseModel):`
    - core/src/core/domain/entities/metric.py contains `class MetricDataPoint(BaseModel):`
    - core/src/core/domain/entities/investigation.py contains `class Investigation(BaseModel):`
    - core/src/core/domain/entities/investigation.py contains `class InvestigationStep(BaseModel):`
    - core/src/core/domain/entities/investigation.py contains `class InvestigatorResult(BaseModel):`
    - core/src/core/domain/entities/investigation.py contains `def start(`
    - core/src/core/domain/entities/investigation.py contains `def complete(`
    - core/src/core/domain/entities/investigation.py contains `def fail(`
    - core/src/core/domain/entities/investigation.py contains `PrivateAttr`
    - core/src/core/domain/entities/insight.py contains `class Insight(BaseModel):`
    - core/src/core/domain/entities/insight.py contains `metadata: dict[str, Any]`
    - core/src/core/domain/entities/thread.py contains `class Thread(BaseModel):`
    - core/src/core/domain/entities/thread.py contains `class Message(BaseModel):`
    - core/src/core/domain/entities/thread.py contains `frozen=True` (on Message)
    - core/src/core/domain/entities/invocation.py contains `class Invocation(BaseModel):`
    - core/src/core/domain/entities/invocation.py contains `def mark_processing(`
    - core/src/core/domain/entities/invocation.py contains `def mark_done(`
    - core/tests/domain/test_entities.py exists and contains at least 15 test functions
    - core/tests/domain/test_events.py exists and contains tests for all 6 events
    - `cd core && uv run pytest tests/domain/ -x -q` exits 0
  </acceptance_criteria>
  <done>All 6 entity files, 3 value object types, test suites pass. Entities support event collection, composite-key equality, state transitions with validation.</done>
</task>

</tasks>

<verification>
- `cd core && uv run pytest tests/domain/ -x -q` -- all domain tests pass
- `cd core && uv run python -c "from core.domain.entities import Stream, Metric, Investigation, Insight, Thread, Invocation"` -- all entities importable
- Directory structure matches: core/src/core/domain/{entities,events,enums.py,exceptions.py} + core/src/core/application/{ports,use_cases}
</verification>

<success_criteria>
- All domain entities instantiable with valid data
- Composite-key entities (Stream, Metric) have correct equality/hash
- Investigation state machine transitions validated
- All domain events frozen and auto-timestamped
- Exception hierarchy with typed context fields
- All tests pass with zero infrastructure dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-domain-ports/01-01-SUMMARY.md`
</output>
