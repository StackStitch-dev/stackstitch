---
phase: 01-core-domain-ports
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - core/src/core/application/ports/repositories.py
  - core/src/core/application/ports/event_publisher.py
  - core/src/core/application/ports/investigator.py
  - core/src/core/application/ports/metrics_calculator.py
  - core/src/core/application/ports/metric_monitor.py
  - core/src/core/application/ports/agent.py
  - core/src/core/application/ports/message_deliverer.py
  - core/src/core/application/ports/__init__.py
  - core/tests/fakes/repositories.py
  - core/tests/fakes/event_publisher.py
  - core/tests/fakes/investigator.py
  - core/tests/fakes/metrics_calculator.py
  - core/tests/fakes/metric_monitor.py
  - core/tests/fakes/agent.py
  - core/tests/fakes/message_deliverer.py
  - core/tests/fakes/__init__.py
  - core/tests/conftest.py
autonomous: true
requirements:
  - INFR-05

must_haves:
  truths:
    - "All port interfaces are abstract base classes with async methods"
    - "Every port has a corresponding in-memory fake that passes type checking"
    - "In-memory fakes are usable in tests without any external dependencies"
    - "Repository ports have save and lookup methods appropriate to their aggregate"
  artifacts:
    - path: "core/src/core/application/ports/repositories.py"
      provides: "All repository port ABCs"
      exports: ["StreamRepository", "MetricRepository", "InsightRepository", "InvestigationRepository", "ThreadRepository", "InvocationRepository"]
    - path: "core/src/core/application/ports/event_publisher.py"
      provides: "EventPublisher ABC"
      exports: ["EventPublisher"]
    - path: "core/src/core/application/ports/investigator.py"
      provides: "Investigator ABC"
      exports: ["Investigator"]
    - path: "core/src/core/application/ports/agent.py"
      provides: "Agent ABC"
      exports: ["Agent"]
    - path: "core/tests/fakes/repositories.py"
      provides: "In-memory repository implementations"
      contains: "InMemoryStreamRepository"
  key_links:
    - from: "core/tests/fakes/repositories.py"
      to: "core/src/core/application/ports/repositories.py"
      via: "fakes inherit from port ABCs"
      pattern: "class InMemory.*Repository\\(.*Repository\\)"
    - from: "core/src/core/application/ports/repositories.py"
      to: "core/src/core/domain/entities/*.py"
      via: "ports reference domain entities in signatures"
      pattern: "from core\\.domain\\.entities"
---

<objective>
Define all secondary port interfaces (ABCs) and create in-memory fakes for every port, enabling use case testing with zero infrastructure.

Purpose: Ports are the hexagonal architecture contracts that adapters implement in Phase 2+. In-memory fakes let Plan 03 test all use cases without MongoDB, Kafka, or any external dependency.
Output: Complete port definitions and test-ready fakes with shared pytest fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-domain-ports/01-CONTEXT.md
@.planning/phases/01-core-domain-ports/01-RESEARCH.md
@.planning/phases/01-core-domain-ports/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port interface definitions (all secondary ports)</name>
  <files>
    core/src/core/application/ports/repositories.py,
    core/src/core/application/ports/event_publisher.py,
    core/src/core/application/ports/investigator.py,
    core/src/core/application/ports/metrics_calculator.py,
    core/src/core/application/ports/metric_monitor.py,
    core/src/core/application/ports/agent.py,
    core/src/core/application/ports/message_deliverer.py,
    core/src/core/application/ports/__init__.py
  </files>
  <read_first>
    .planning/phases/01-core-domain-ports/01-CONTEXT.md,
    core/src/core/domain/entities/stream.py,
    core/src/core/domain/entities/metric.py,
    core/src/core/domain/entities/insight.py,
    core/src/core/domain/entities/investigation.py,
    core/src/core/domain/entities/thread.py,
    core/src/core/domain/entities/invocation.py,
    core/src/core/domain/enums.py,
    core/src/core/domain/events/domain_events.py
  </read_first>
  <action>
    All ports use `from __future__ import annotations`, abc.ABC, and `@abc.abstractmethod`. All methods are `async def` per D-35.

    1. core/src/core/application/ports/repositories.py -- All repository ports in one file per D-35:

    StreamRepository(abc.ABC):
      - async save(self, stream: Stream) -> None
      - async get_by_key(self, source: str, stream_type: StreamType, project_id: UUID) -> Stream | None

    MetricRepository(abc.ABC):
      - async save(self, metric: Metric) -> None
      - async get_by_key(self, metric_type: MetricType, project_id: UUID) -> Metric | None

    InsightRepository(abc.ABC):
      - async save(self, insight: Insight) -> None
      - async get_by_id(self, insight_id: UUID) -> Insight | None

    InvestigationRepository(abc.ABC):
      - async save(self, investigation: Investigation) -> None
      - async get_by_id(self, investigation_id: UUID) -> Investigation | None

    ThreadRepository(abc.ABC):
      - async save(self, thread: Thread) -> None
      - async get_by_id(self, thread_id: UUID) -> Thread | None

    InvocationRepository(abc.ABC):
      - async save(self, invocation: Invocation) -> None
      - async save_many(self, invocations: list[Invocation]) -> None
      - async get_by_id(self, invocation_id: UUID) -> Invocation | None
      - async get_pending_by_thread_id(self, thread_id: UUID) -> list[Invocation]
        (needed by Orchestrate drain loop per D-30)

    2. core/src/core/application/ports/event_publisher.py per D-37:
    EventPublisher(abc.ABC):
      - async publish(self, event: DomainEvent) -> None
      - async publish_many(self, events: list[DomainEvent]) -> None

    3. core/src/core/application/ports/investigator.py per D-67:
    Investigator(abc.ABC):
      - async investigate(self, investigation: Investigation, context: dict[str, Any]) -> InvestigatorResult

    4. core/src/core/application/ports/metrics_calculator.py per D-23:
    MetricsCalculator(abc.ABC):
      - async calculate(self, stream: Stream) -> list[MetricDataPoint]
        (returns new data points computed from stream data)

    5. core/src/core/application/ports/metric_monitor.py per D-24:
    MetricMonitor(abc.ABC):
      - async check(self, metric: Metric) -> None
        (returns nothing -- emits AnomalyDetected via internal EventPublisher per D-24)

    6. core/src/core/application/ports/agent.py per D-68:
    Agent(abc.ABC):
      - async process(self, thread: Thread, invocations: list[Invocation]) -> str

    7. core/src/core/application/ports/message_deliverer.py per D-32:
    MessageDeliverer(abc.ABC):
      - async deliver(self, thread_id: UUID, message: str) -> None

    8. core/src/core/application/ports/__init__.py -- re-export all port classes.
  </action>
  <verify>
    <automated>cd /Users/jorgeandresdiaz/Documents/development/stackstitch/core && uv run python -c "from core.application.ports import StreamRepository, MetricRepository, InsightRepository, InvestigationRepository, ThreadRepository, InvocationRepository, EventPublisher, Investigator, MetricsCalculator, MetricMonitor, Agent, MessageDeliverer; print('All ports importable')"</automated>
  </verify>
  <acceptance_criteria>
    - core/src/core/application/ports/repositories.py contains `class StreamRepository(abc.ABC):`
    - core/src/core/application/ports/repositories.py contains `class InvocationRepository(abc.ABC):`
    - core/src/core/application/ports/repositories.py contains `async def get_pending_by_thread_id(`
    - core/src/core/application/ports/repositories.py contains `async def get_by_key(` (for Stream and Metric repos)
    - core/src/core/application/ports/event_publisher.py contains `class EventPublisher(abc.ABC):`
    - core/src/core/application/ports/event_publisher.py contains `async def publish(`
    - core/src/core/application/ports/event_publisher.py contains `async def publish_many(`
    - core/src/core/application/ports/investigator.py contains `class Investigator(abc.ABC):`
    - core/src/core/application/ports/investigator.py contains `async def investigate(`
    - core/src/core/application/ports/metrics_calculator.py contains `class MetricsCalculator(abc.ABC):`
    - core/src/core/application/ports/metric_monitor.py contains `class MetricMonitor(abc.ABC):`
    - core/src/core/application/ports/agent.py contains `class Agent(abc.ABC):`
    - core/src/core/application/ports/agent.py contains `async def process(`
    - core/src/core/application/ports/message_deliverer.py contains `class MessageDeliverer(abc.ABC):`
    - All port methods are `async def` (no sync methods)
    - All port classes inherit from `abc.ABC`
    - All methods decorated with `@abc.abstractmethod`
  </acceptance_criteria>
  <done>All 7 port files define abstract contracts. Every method is async. Ports are importable from core.application.ports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: In-memory fakes and pytest fixtures</name>
  <files>
    core/tests/fakes/repositories.py,
    core/tests/fakes/event_publisher.py,
    core/tests/fakes/investigator.py,
    core/tests/fakes/metrics_calculator.py,
    core/tests/fakes/metric_monitor.py,
    core/tests/fakes/agent.py,
    core/tests/fakes/message_deliverer.py,
    core/tests/fakes/__init__.py,
    core/tests/conftest.py
  </files>
  <read_first>
    core/src/core/application/ports/repositories.py,
    core/src/core/application/ports/event_publisher.py,
    core/src/core/application/ports/investigator.py,
    core/src/core/application/ports/metrics_calculator.py,
    core/src/core/application/ports/metric_monitor.py,
    core/src/core/application/ports/agent.py,
    core/src/core/application/ports/message_deliverer.py,
    core/src/core/domain/entities/stream.py,
    core/src/core/domain/entities/investigation.py
  </read_first>
  <behavior>
    - InMemoryStreamRepository.save stores stream, get_by_key retrieves by composite key
    - InMemoryMetricRepository.save stores metric, get_by_key retrieves by composite key
    - InMemoryInsightRepository.save/get_by_id works correctly
    - InMemoryInvestigationRepository.save/get_by_id works correctly
    - InMemoryThreadRepository.save/get_by_id works correctly
    - InMemoryInvocationRepository.save/get_by_id works, get_pending_by_thread_id filters by thread_id AND status=PENDING
    - InMemoryInvocationRepository.save_many stores multiple invocations
    - InMemoryEventPublisher.publish appends to events list, publish_many extends
    - FakeInvestigator.investigate returns a preset InvestigatorResult
    - FakeMetricsCalculator.calculate returns a preset list of MetricDataPoints
    - FakeMetricMonitor.check records the call (stores metric for assertion)
    - FakeAgent.process returns a preset response string
    - FakeMessageDeliverer.deliver records delivery (stores thread_id + message)
    - All fakes inherit from their corresponding ABC port
  </behavior>
  <action>
    Write a small test file (core/tests/test_fakes.py) that verifies each fake works correctly (RED first), then implement all fakes (GREEN).

    1. core/tests/fakes/repositories.py:
    - InMemoryStreamRepository(StreamRepository): stores in dict keyed by tuple (source, stream_type, project_id)
    - InMemoryMetricRepository(MetricRepository): stores in dict keyed by tuple (metric_type, project_id)
    - InMemoryInsightRepository(InsightRepository): stores in dict[UUID, Insight]
    - InMemoryInvestigationRepository(InvestigationRepository): stores in dict[UUID, Investigation]
    - InMemoryThreadRepository(ThreadRepository): stores in dict[UUID, Thread]
    - InMemoryInvocationRepository(InvocationRepository): stores in dict[UUID, Invocation], get_pending_by_thread_id filters where invocation.thread_id == thread_id and invocation.status == InvocationStatus.PENDING

    2. core/tests/fakes/event_publisher.py:
    - InMemoryEventPublisher(EventPublisher): events: list[DomainEvent] = [], publish appends, publish_many extends, add clear() helper for test resets

    3. core/tests/fakes/investigator.py:
    - FakeInvestigator(Investigator): takes preset_result: InvestigatorResult in __init__, investigate returns it. Also stores calls: list of (investigation, context) tuples for assertion.

    4. core/tests/fakes/metrics_calculator.py:
    - FakeMetricsCalculator(MetricsCalculator): takes preset_results: list[MetricDataPoint] in __init__, calculate returns them. Stores calls for assertion.

    5. core/tests/fakes/metric_monitor.py:
    - FakeMetricMonitor(MetricMonitor): check stores the metric in checked_metrics: list[Metric]. Does not emit events (that's the real adapter's job via EventPublisher).

    6. core/tests/fakes/agent.py:
    - FakeAgent(Agent): takes preset_response: str in __init__, process returns it. Stores calls: list of (thread, invocations) tuples.

    7. core/tests/fakes/message_deliverer.py:
    - FakeMessageDeliverer(MessageDeliverer): deliver stores (thread_id, message) in deliveries: list.

    8. core/tests/fakes/__init__.py: re-export all fakes.

    9. core/tests/conftest.py: Create pytest fixtures that instantiate each fake:
    - event_publisher() -> InMemoryEventPublisher
    - stream_repo() -> InMemoryStreamRepository
    - metric_repo() -> InMemoryMetricRepository
    - insight_repo() -> InMemoryInsightRepository
    - investigation_repo() -> InMemoryInvestigationRepository
    - thread_repo() -> InMemoryThreadRepository
    - invocation_repo() -> InMemoryInvocationRepository
    - investigator(preset InvestigatorResult) -> FakeInvestigator
    - metrics_calculator(preset empty list) -> FakeMetricsCalculator
    - metric_monitor() -> FakeMetricMonitor
    - agent(preset "test response") -> FakeAgent
    - message_deliverer() -> FakeMessageDeliverer

    10. Create core/tests/test_fakes.py with tests verifying each fake's basic operations (save+retrieve, publish+check list, etc.).
  </action>
  <verify>
    <automated>cd /Users/jorgeandresdiaz/Documents/development/stackstitch/core && uv run pytest tests/test_fakes.py tests/domain/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - core/tests/fakes/repositories.py contains `class InMemoryStreamRepository(StreamRepository):`
    - core/tests/fakes/repositories.py contains `class InMemoryInvocationRepository(InvocationRepository):`
    - core/tests/fakes/repositories.py contains `async def get_pending_by_thread_id(`
    - core/tests/fakes/event_publisher.py contains `class InMemoryEventPublisher(EventPublisher):`
    - core/tests/fakes/investigator.py contains `class FakeInvestigator(Investigator):`
    - core/tests/fakes/metrics_calculator.py contains `class FakeMetricsCalculator(MetricsCalculator):`
    - core/tests/fakes/metric_monitor.py contains `class FakeMetricMonitor(MetricMonitor):`
    - core/tests/fakes/agent.py contains `class FakeAgent(Agent):`
    - core/tests/fakes/message_deliverer.py contains `class FakeMessageDeliverer(MessageDeliverer):`
    - core/tests/conftest.py contains at least 8 `@pytest.fixture` decorators
    - core/tests/test_fakes.py exists with tests for repository save/retrieve, event publisher, and fake call recording
    - `cd core && uv run pytest tests/test_fakes.py -x -q` exits 0
    - `cd core && uv run pytest tests/ -x -q` exits 0 (full suite still passes)
  </acceptance_criteria>
  <done>All ports have in-memory fakes. Fakes pass their own tests. Pytest fixtures provide pre-wired fakes for use case testing in Plan 03.</done>
</task>

</tasks>

<verification>
- `cd core && uv run pytest tests/ -x -q` -- all tests pass (domain + fakes)
- `cd core && uv run python -c "from core.application.ports import StreamRepository, EventPublisher, Investigator, Agent; from tests.fakes import InMemoryStreamRepository, InMemoryEventPublisher"` -- ports and fakes importable
- Each fake class inherits from its corresponding ABC port (verifiable by isinstance checks)
</verification>

<success_criteria>
- All 7 port categories defined as ABCs with async methods
- All ports have corresponding in-memory fakes
- Fakes pass their own test suite
- Shared pytest fixtures ready for use case testing
- Zero infrastructure dependencies in any test
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-domain-ports/01-02-SUMMARY.md`
</output>
